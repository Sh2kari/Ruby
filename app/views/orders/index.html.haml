%h1 Listing Orders
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - @orders.each do |order|
      - order.order_products.each do |order|
        %tr
          %td= order.product.title
