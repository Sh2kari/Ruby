%ul.nav.navbar-nav
- @products.each do |category, products|
  %li= category.name
  %ul
    - products.each do |product|
      %li= product.title.humanize
      %li
        = number_to_currency(product.price, locale: :ru)
  %hr

= link_to 'New Product', new_weekday_product_path

