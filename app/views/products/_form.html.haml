.box-body
  .form-group
    = simple_form_for [weekday, product], html: { multipart: true } do |f|
      = f.association :category, :include_blank => false
      = f.input :title
      = f.input :price
      = f.button :submit, class: 'btn btn-primary'
